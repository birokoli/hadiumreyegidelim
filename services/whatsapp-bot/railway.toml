[build]
builder = "DOCKERFILE"
dockerfilePath = "services/whatsapp-bot/Dockerfile"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
